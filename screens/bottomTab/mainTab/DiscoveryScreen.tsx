import { MainStackScreenProps } from "../../../types/screens";
import { Text } from '../../../components/themed';

export default function DiscoveryScreen({ navigation }: MainStackScreenProps<'DiscoveryScreen'>) {
    return (
        <Text>Discovery Screen</Text>
    )
}